 <div class="navbar navbar-fixed-top">
  <div class="navbar">
    <div class="navbar-inner">
      <div class="container" style="width: auto;">
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <%= link_to "健身教练Coach", root_path, :class => "brand" %>
        <div class="nav-collapse">
          <ul class="nav">
            <li><%= link_to "主页", root_path %></li>
            <% unless user_signed_in? %>
              <li><%= link_to('登录', new_user_session_path)  %></li>
            <% end -%>
		        <li><%= link_to "网站规划", plan_home_index_path %></li>
          </ul>
          <form class="navbar-search pull-left" action="">
            <input type="text" class="search-query span2" placeholder="Search">
          </form>
          <ul class="nav pull-right">
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">帐号<b class="caret"></b></a>
              <ul class="dropdown-menu">
              <% if user_signed_in? %>
                <li><%= link_to('注销', destroy_user_session_path, :method=>'delete') %></li>
              <% else %>
                <li><%= link_to('登录', new_user_session_path)  %></li>
              <% end %>
              <% if user_signed_in? %>
                <li><%= link_to('编辑账户', edit_user_registration_path) %></li>
              <% else %>
                <li><%= link_to('注册', new_user_registration_path)  %></li>
              <% end %>
              </ul>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</div>



</div>