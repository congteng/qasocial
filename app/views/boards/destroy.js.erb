$('.display_area').html("<%= recursion_board_without_button @user.boards(:board_id => nil)%>")
$('.display_area_btn').html("<%= recursion_board_with_button @user.boards(:board_id => nil) %>")

$('.labels').html("<%= j render 'functionalities/labels'%>")